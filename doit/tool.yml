# Name must match the class name in __init__.py
tool: Doit
namespace: doit 

# Dictionary containg all properties/attributes required by the tool
properties:
  verbosity:
    description: Verbosity level for doit (see doit )
    default: 2
    schema: "enum(0, 1, 2)"
